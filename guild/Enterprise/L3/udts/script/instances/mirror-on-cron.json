{
  "udtType": "Script",
  "instance": "mirror-refresh-on-cron",
  "parameters": {
    "id":      "mirror-refresh:on-cron",
    "enabled": true,
    "trigger": { "kind": "on_cron", "spec": "*/15 * * * *" },
    "action":  { "tool_id": "build:svg:fork-compare", "inputs": {} }
  }
}
