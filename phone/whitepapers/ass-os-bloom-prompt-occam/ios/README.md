# 🌸 ASS-OS BLOOM PROMPT — OCCAM · iOS

One-screen SwiftUI app. Part of the ACG whitepaper library.

```bash
brew install xcodegen
xcodegen generate
open AssOsBloomPromptOccam.xcodeproj
```

Uses Xcode 16 (iOS 17 deployment target). Bundle id
`com.aicraftspeopleguild.acg.papers.assosbloompromptoccam`. Ships to the App Store as an independent app —
every paper gets its own listing.
