{
  "udtType": "Script",
  "instance": "mirror-refresh-on-push",
  "parameters": {
    "id":      "mirror-refresh:on-push",
    "enabled": true,
    "trigger": { "kind": "on_push", "spec": { "branches": ["main","feat/mirror-compare"] } },
    "action":  { "tool_id": "build:svg:fork-compare", "inputs": {} }
  }
}
