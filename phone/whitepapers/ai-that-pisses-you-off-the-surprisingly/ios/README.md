# AI That Pisses You Off: The Surprisingly Effective Path to Deeper Thinking · iOS

One-screen SwiftUI app. Part of the ACG whitepaper library.

```bash
brew install xcodegen
xcodegen generate
open AiThatPissesYouOffTheSurprisingly.xcodeproj
```

Uses Xcode 16 (iOS 17 deployment target). Bundle id
`com.aicraftspeopleguild.acg.papers.aithatpissesyouoffthesurprisingly`. Ships to the App Store as an independent app —
every paper gets its own listing.
