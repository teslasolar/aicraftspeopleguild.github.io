{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "udtType": "AndroidAtom.StatRow",
  "version": "1.0.0",
  "description": "label/value pair. Renders as one Row with the label on the left and the value on the right. Composed by StatCard.",
  "parameters": {
    "label": { "dataType": "String", "required": true },
    "value": { "dataType": "String", "required": true },
    "valueColor": { "dataType": "String", "description": "theme slot name: text | primary | error | dim", "default": "text" }
  },
  "tagRoot": "android.atom.statrow.{id}"
}
